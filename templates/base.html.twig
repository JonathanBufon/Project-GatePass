<!doctype html>
<html lang="pt-br" class="h-100">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="icon" href="{{ asset('assets/css/registro/style.css') }}">

    <title>{% block title %}GatePass - Venda de Ingressos{% endblock %}</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">

    <link rel="stylesheet" href="{{ asset('assets/css/app.css') }}">

    {% block stylesheets %}
        {#
        * CORREÇÃO 1: Centralizando o 'padding-top'
        * Aplicamos a compensação do 'fixed-top' navbar globalmente no 'body'.
        * Agora, todos os templates filhos (incluindo o dashboard) herdarão
        * isso automaticamente, e podemos remover a regra duplicada do 'dashboard.css'.
        #}
        <style>
            body {
                /* Ajuste este valor se a altura da navbar mudar */
                padding-top: 60px;
            }
        </style>
    {% endblock %}
</head>

<body class="d-flex flex-column h-100 {% block body_class %}{% endblock %}">

{# === Bloco de Cabeçalho (Navbar Padrão) === #}
{% block header %}
    <header>
        {# A altura da navbar (aprox. 60px) define o padding-top do <body> acima #}
        <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
            <div class="container-fluid">
                <a class="navbar-brand" href="{{ path('app_home') }}">GatePass</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarCollapse">
                    <ul class="navbar-nav me-auto mb-2 mb-md-0">
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page" href="{{ path('app_home') }}">Eventos</a>
                        </li>

                        {#
                        * CORREÇÃO 2: Lógica de Role Explícita
                        * Verificamos 'ROLE_VENDEDOR' primeiro.
                        * Para 'Cliente', verificamos se é 'ROLE_USER' mas NÃO 'ROLE_VENDEDOR'.
                        #}
                        {% if is_granted('ROLE_VENDEDOR') %}
                            {# Se for Vendedor, link para o Dashboard #}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_vendedor_dashboard') }}">Dashboard Vendedor</a>
                            </li>
                        {% elseif is_granted('ROLE_USER') and not is_granted('ROLE_VENDEDOR') %}
                            {# Se for apenas Cliente, link para Meus Pedidos #}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_cliente_pedidos_index') }}">Meus Pedidos</a>
                            </li>
                        {% endif %}
                    </ul>
                    <div class="d-flex">
                        {% if app.user %}
                            <span class="navbar-text me-2">
                                Olá, {{ app.user.userIdentifier }}
                             </span>
                            <a href="{{ path('app_logout') }}" class="btn btn-outline-warning">Sair</a>
                        {% else %}
                            <a href="{{ path('app_login') }}" class="btn btn-outline-success me-2">Login</a>
                            <a href="{{ path('app_registro') }}" class="btn btn-success">Registrar</a>
                        {% endif %}
                    </div>
                </div>
            </div>
        </nav>
    </header>
{% endblock %}


{# === Bloco de Conteúdo Principal === #}
{% block main_content_wrapper %}
    <main class="flex-shrink-0">
        {# CORREÇÃO 1: O 'style="padding-top: 60px;"' foi removido daqui #}
        <div class="container">

            {# Inclui as flash messages (SRP) em container fixo de toasts #}
            <div class="toast-container">
                {% include '_partials/flash_messages.html.twig' %}
            </div>

            {% block main_content %}{% endblock %}
        </div>
    </main>
{% endblock %}


{# === Bloco de Rodapé (Padrão) === #}
{% block footer %}
    <footer class="footer mt-auto py-3 bg-light">
        <div class="container">
            <span class="text-muted">&copy; {{ "now"|date("Y") }} GatePass. Todos os direitos reservados.</span>
        </div>
    </footer>
{% endblock %}


<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-kenU1KFdBIe4zVF0s0G1M5b4hcpxyD9F7jL+jjXkk+Q2h455rYXK/7HAuoJl+0I4" crossorigin="anonymous"></script>

<script src="{{ asset('assets/js/app.js') }}"></script>

{% block javascripts %}{% endblock %}

</body>
</html>