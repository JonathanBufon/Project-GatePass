{% extends 'base.html.twig' %}
{% block title %}GatePass — Seu acesso aos melhores eventos{% endblock %}
{% block main_content %}

<div id="homeCarousel" class="carousel slide mb-5" data-bs-ride="carousel">
  <div class="carousel-indicators">
    {% for b in banners %}
      <button type="button" data-bs-target="#homeCarousel" data-bs-slide-to="{{ loop.index0 }}" class="{% if loop.first %}active{% endif %}" aria-current="{{ loop.first ? 'true' : 'false' }}" aria-label="Slide {{ loop.index }}"></button>
    {% endfor %}
  </div>
  <div class="carousel-inner rounded-md shadow-1">
    {% for b in banners %}
      <div class="carousel-item {% if loop.first %}active{% endif %}">
        <img src="{{ b.urlBanner ?? 'https://via.placeholder.com/1200x450?text=GatePass' }}" class="d-block w-100" alt="{{ b.nome|default('Banner') }}" {% if not loop.first %}loading="lazy"{% endif %}>
        <div class="container">
          <div class="carousel-caption text-start">
            <h1>GatePass: O seu acesso direto aos melhores eventos.</h1>
            <p>Encontre, compre e aproveite experiências inesquecíveis...</p>
            <p><a class="btn btn-lg btn-primary" href="{{ path('app_evento_index') }}">Ver Eventos Agora</a></p>
          </div>
        </div>
      </div>
    {% else %}
      <div class="carousel-item active">
        <img src="https://via.placeholder.com/1200x450?text=GatePass" class="d-block w-100" alt="GatePass">
        <div class="container">
          <div class="carousel-caption text-start">
            <h1>GatePass: O seu acesso direto aos melhores eventos.</h1>
            <p>Encontre, compre e aproveite experiências inesquecíveis...</p>
            <p><a class="btn btn-lg btn-primary" href="{{ path('app_evento_index') }}">Ver Eventos Agora</a></p>
          </div>
        </div>
      </div>
    {% endfor %}
  </div>
  <button class="carousel-control-prev" type="button" data-bs-target="#homeCarousel" data-bs-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="visually-hidden">Previous</span>
  </button>
  <button class="carousel-control-next" type="button" data-bs-target="#homeCarousel" data-bs-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="visually-hidden">Next</span>
  </button>
  <div class="mt-3 text-center">
    <a href="{{ path('app_evento_index') }}" class="btn btn-outline-light">Ver todos os eventos</a>
  </div>
  
</div>

<div class="container marketing">
  <div class="row text-center mb-5">
    <div class="col-lg-4">
      <h2 class="fw-normal">Encontre</h2>
      <p class="text-muted">Explore centenas de eventos com filtros e categorias.</p>
    </div>
    <div class="col-lg-4">
      <h2 class="fw-normal">Compre com Segurança</h2>
      <p class="text-muted">Checkout simples, rápido e seguro, do seu jeito.</p>
    </div>
    <div class="col-lg-4">
      <h2 class="fw-normal">Aproveite o Momento</h2>
      <p class="text-muted">Receba o acesso e aproveite sem preocupações.</p>
    </div>
  </div>

  <hr class="my-5"/>

  <h2 class="mb-4">Eventos em Destaque</h2>
  <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 row-cols-lg-4 g-3">
    {% for evento in eventosDestaque %}
      <div class="col">
        <div class="card h-100 shadow-sm">
          <img src="{{ evento.urlBanner|default('https://via.placeholder.com/800x400?text=GatePass') }}" class="card-img-top" alt="{{ evento.nome }}" loading="lazy">
          <div class="card-body d-flex flex-column">
            <h5 class="card-title">{{ evento.nome }}</h5>
            <p class="card-text text-muted">{{ evento.local }} • {{ evento.dataHoraInicio|date('d/m/Y H:i') }}</p>
            <div class="mt-auto">
              <a href="{{ path('app_evento_detalhe', {id: evento.id}) }}" class="btn btn-primary btn-sm">Ver detalhes</a>
            </div>
          </div>
        </div>
      </div>
    {% else %}
      <div class="col-12"><div class="text-muted">Nenhum evento em destaque no momento.</div></div>
    {% endfor %}
  </div>

  <hr class="my-5"/>

  <div class="row featurette align-items-center">
    <div class="col-md-7">
      <h2 class="featurette-heading fw-normal lh-1">Venda seus ingressos no GatePass.</h2>
      <p class="lead text-muted">Crie seus eventos com facilidade, gerencie lotes e alcance mais público com a nossa plataforma.</p>
      <a href="{{ path('app_registro_vendedor') }}" class="btn btn-primary btn-lg">Começar agora</a>
    </div>
    <div class="col-md-5">
      <svg class="bd-placeholder-img bd-placeholder-img-lg featurette-image img-fluid mx-auto" width="500" height="500" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Placeholder" preserveAspectRatio="xMidYMid slice" focusable="false"><title>Placeholder</title><rect width="100%" height="100%" fill="#4b44ab"/></svg>
    </div>
  </div>

</div>

{% endblock %}
