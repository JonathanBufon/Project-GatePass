<nav class="navbar navbar-expand-md navbar-dark bg-dark">
  <div class="container">
    <a class="navbar-brand d-flex align-items-center" href="{{ path('app_home') }}">
      <img src="{{ asset('assets/img/logo-gatepass.svg') }}" alt="GatePass Logo" width="28" height="28" class="me-2">
      <span>GatePass</span>
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navMain" aria-controls="navMain" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navMain">
      <ul class="navbar-nav me-auto mb-2 mb-md-0">
        <li class="nav-item"><a class="nav-link" href="{{ path('app_home') }}">Eventos</a></li>
        {% if is_granted('ROLE_VENDEDOR') %}
          <li class="nav-item"><a class="nav-link" href="{{ path('app_vendedor_dashboard') }}">Dashboard</a></li>
        {% elseif is_granted('ROLE_USER') %}
          <li class="nav-item"><a class="nav-link" href="{{ path('app_cliente_pedidos_index') }}">Meus Pedidos</a></li>
        {% endif %}
      </ul>
      <div class="d-flex">
        {% if app.user %}
          <span class="navbar-text me-2">Olá, {{ app.user.userIdentifier }}</span>
          <a class="btn btn-outline-warning" href="{{ path('app_logout') }}">Sair</a>
        {% else %}
          <a class="btn btn-outline-light me-2" href="{{ path('app_login') }}">Login</a>
          <a class="btn btn-outline-light me-2" href="{{ path('app_registro') }}">Cadastre-se</a>
          <a class="btn btn-primary" href="{{ path('app_registro_vendedor') }}">Seja um Vendedor</a>
        {% endif %}
      </div>
    </div>
  </div>
</nav>
